# Built canvas: the six .dc.html sources plus canvas.json are the design.
# This is those, wrapped around a 2 MB copy of the editor, and it is
# regenerated by seed-canvas.mjs whenever the sources change. Keeping it
# in the repo would mean a two-megabyte diff every time a colour moves.
scems-field-training-screens.html
